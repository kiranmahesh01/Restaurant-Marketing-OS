'use client';
import { useState } from 'react';
export default function Onboarding(){
 const [name,setName]=useState(''),[city,setCity]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);
 async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{const r=await fetch('/api/onboarding',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,city})});const d=await r.json();if(r.status===401){window.location.assign('/login');return;}if(!r.ok)throw new Error(d.error);window.location.assign('/dashboard');}catch(e){setMessage(e instanceof Error?e.message:'Unable to save');}finally{setBusy(false);}}
 return <main className="flex min-h-screen items-center justify-center bg-ink-50 p-6"><form onSubmit={submit} className="w-full max-w-md space-y-5 rounded-2xl bg-white p-8 shadow-soft"><h1 className="text-2xl font-semibold">Set up your restaurant</h1><p className="text-sm text-ink-500">Your workspace starts empty. Add your customers, offers and content when ready.</p><label className="block">Restaurant name<input required maxLength={120} value={name} onChange={e=>setName(e.target.value)} className="mt-1 w-full rounded-lg border p-3"/></label><label className="block">City<input required maxLength={100} value={city} onChange={e=>setCity(e.target.value)} className="mt-1 w-full rounded-lg border p-3"/></label>{message&&<p role="alert" className="text-red-700">{message}</p>}<button disabled={busy} className="w-full rounded-lg bg-ink-900 p-3 text-white">{busy?'Creating…':'Create restaurant'}</button></form></main>;
}
